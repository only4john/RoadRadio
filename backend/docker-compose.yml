# docker-compose.yml — Laniakea Radio 后端一键部署
version: "3.9"

services:
  radio-backend:
    build: .
    container_name: laniakea-radio
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # SQLite 数据持久化
    restart: unless-stopped
